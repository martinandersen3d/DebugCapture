<UserControl x:Class="DebugCapture.ToolWindows.SnapshotToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:DebugCapture.Models"
             xmlns:vs="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             Background="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
             Foreground="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowTextBrushKey}}"
             MinWidth="640"
             MinHeight="360">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowTextBrushKey}}" />
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowTextBrushKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBorderBrushKey}}" />
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowTextBrushKey}}" />
        </Style>
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowTextBrushKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBorderBrushKey}}" />
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowTextBrushKey}}" />
        </Style>
        <Style TargetType="Expander">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowTextBrushKey}}" />
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
        </Style>
        <Style TargetType="GridSplitter">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBorderBrushKey}}" />
        </Style>
        <HierarchicalDataTemplate DataType="{x:Type models:SnapshotProperty}" ItemsSource="{Binding Children}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="220" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Name}" FontFamily="Consolas" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="1" Text="{Binding Value}" FontFamily="Consolas" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="2" Text="{Binding Type}" FontFamily="Consolas" TextTrimming="CharacterEllipsis" />
            </Grid>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <DockPanel LastChildFill="True" Background="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBackgroundBrushKey}}">
        <Border DockPanel.Dock="Top" BorderBrush="{DynamicResource {x:Static vs:EnvironmentColors.ToolWindowBorderBrushKey}}" BorderThickness="0,0,0,1" Padding="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="36" />
                    <ColumnDefinition Width="36" />
                    <ColumnDefinition Width="36" />
                    <ColumnDefinition Width="36" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="36" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="┃◀" Command="{Binding FirstCommand}" ToolTip="First snapshot" Margin="0,0,4,0" />
                <Button Grid.Column="1" Content="◀" Command="{Binding PreviousCommand}" ToolTip="Previous snapshot" Margin="0,0,4,0" />
                <Button Grid.Column="2" Content="▶" Command="{Binding NextCommand}" ToolTip="Next snapshot" Margin="0,0,4,0" />
                <Button Grid.Column="3" Content="▶┃" Command="{Binding LastCommand}" ToolTip="Last snapshot" Margin="0,0,8,0" />
                <TextBlock Grid.Column="4" Text="Snapshot Number:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <Slider Grid.Column="5" Minimum="0" Maximum="{Binding MaxSnapshotIndex}" Value="{Binding State.SelectedIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="SnapshotSlider_ValueChanged" VerticalAlignment="Center" />
                <TextBlock Grid.Column="6" Text="{Binding State.SelectedIndex}" VerticalAlignment="Center" Margin="8,0,0,0" />
                <TextBlock Grid.Column="7" Text="{Binding State.StatusMessage}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="8,0" />
                <Button Grid.Column="8" Content="⟳" Command="{Binding RefreshCommand}" ToolTip="Refresh snapshots" />
            </Grid>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="150" MinHeight="100" />
                <RowDefinition Height="4" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ListView Grid.Row="0"
                      ItemsSource="{Binding State.Snapshots}"
                      SelectedItem="{Binding State.SelectedSnapshot, Mode=TwoWay}"
                      SelectionChanged="SnapshotList_SelectionChanged"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="28">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelectedForAction, Mode=TwoWay}" ToolTip="Select" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="34">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="◎" ToolTip="Go to Source Code" Click="OpenSource_Click" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="34">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🗁" ToolTip="Windows Explorer: Select Snapshot File" Click="OpenInExplorer_Click" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Location" DisplayMemberBinding="{Binding SourceLocation}" Width="260" />
                        <GridViewColumn Header="Trigger" DisplayMemberBinding="{Binding Trigger}" Width="110" />
                        <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Time}" Width="80" />
                    </GridView>
                </ListView.View>
            </ListView>

            <GridSplitter Grid.Row="1" Height="4" HorizontalAlignment="Stretch" />

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <StackPanel Margin="4">
                    <Expander Header="Locals" IsExpanded="True">
                        <TreeView ItemsSource="{Binding Locals}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                    </Expander>
                    <Expander Header="Autos" IsExpanded="True">
                        <TreeView ItemsSource="{Binding Autos}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                    </Expander>
                    <Expander Header="Watch 1">
                        <TreeView ItemsSource="{Binding Watch1}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                    </Expander>
                    <Expander Header="Watch 2">
                        <TreeView ItemsSource="{Binding Watch2}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                    </Expander>
                    <Expander Header="Watch 3">
                        <TreeView ItemsSource="{Binding Watch3}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                    </Expander>
                    <Expander Header="Watch 4">
                        <TreeView ItemsSource="{Binding Watch4}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                    </Expander>
                    <Expander Header="Exception">
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedSnapshot.Exception.TypeName, StringFormat=Type: {0}}" FontFamily="Consolas" />
                            <TextBlock Text="{Binding SelectedSnapshot.Exception.Message, StringFormat=Message: {0}}" FontFamily="Consolas" TextWrapping="Wrap" />
                            <TreeView ItemsSource="{Binding ExceptionMembers}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                        </StackPanel>
                    </Expander>
                    <Expander Header="Callstack">
                        <ListView ItemsSource="{Binding CallStack}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Function" DisplayMemberBinding="{Binding FunctionName}" Width="260" />
                                    <GridViewColumn Header="File" DisplayMemberBinding="{Binding File}" Width="320" />
                                    <GridViewColumn Header="Line" DisplayMemberBinding="{Binding Line}" Width="60" />
                                    <GridViewColumn Header="Module" DisplayMemberBinding="{Binding Module}" Width="320" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </DockPanel>
</UserControl>
